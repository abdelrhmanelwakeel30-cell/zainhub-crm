{
  "name": "zainhub-crm-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server bridging the ZainHub 166-agent platform to the CRM (per-agent API key, RBAC + audit enforced server-side).",
  "bin": {
    "zainhub-crm-mcp": "index.mjs"
  },
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  }
}
